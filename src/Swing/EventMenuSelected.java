package Swing;

/**
 * Interface for handling menu selection events.
 * Implementing classes define actions when a menu item is selected.
 */

public interface EventMenuSelected {
    void menuSelected(int index, int subIndex);
}
